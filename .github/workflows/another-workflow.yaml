name: "Another Workflow"
on:
  pull_request:
  push:
jobs:
  Test-Job:
    runs-on: ubuntu-latest
  
  steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Pip install
      run: |
        pip install -r requirements.txt