name: "Run Linter"
on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  Linter Checker:
    steps:
      name: 'Checkout'
      uses: actions/Checkout@v4
      
      name: 'Install Dependencies'
      run: |
          pip Install reqirement.txt
